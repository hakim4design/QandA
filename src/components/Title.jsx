import React from 'react'

const Title = ({title}) => {
  return (
    <div className='w-100 d-block text-center'>
        <h1 className='fs-4 text-center'> {title}</h1>
    </div>
  )
}

export default Title